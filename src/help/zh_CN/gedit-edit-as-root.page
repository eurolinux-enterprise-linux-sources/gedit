<?xml version="1.0" encoding="utf-8"?>
<page xmlns="http://projectmallard.org/1.0/" type="topic" style="task" id="gedit-edit-as-root" xml:lang="zh-CN">

  <info>
    <link type="guide" xref="index#gedit-working-with-files"/>
    <revision pkgversion="3.8" date="2013-03-07" status="final"/>
    <credit type="author">
      <name>Jim Campbell</name>
      <email>jwcampbell@gmail.com</email>
    </credit>
  </info>

  <title>Edit a file as the root user</title>

  <note style="warning">
    <p>以 root 身份编辑文件可能带来潜在危险，会因操作失误而损坏系统，请特别小心使用 root 身份编辑文件。</p>
  </note>

  <p>要以 root 身份编辑文件，在终端运行下列命令：</p>
  <code>sudo gedit</code>

  <p>使用  <cmd>sudo</cmd> 命令，正确输入自己的密码后，打开 <app>gedit</app>。</p>

  <p>作为 <cmd>sudo</cmd> 命令的替代，您也可以通过输入下列命令来获取管理员权限：</p>
  <code>su -</code>

  <p>然后您就可以运行并使用 <app>gedit</app>。</p>

  <note style="tip">
    <p>Once you have opened <app>gedit</app> with administrative privileges,
    <app>gedit</app> will keep those privileges until you close it.</p>
  </note>

</page>
